import { SwaggerConfig } from "./swagger.interface";

export const SWAGGER_CONFIG:SwaggerConfig={
    title:'Music Application Api',
    description:'all api in music application',
    version:'1.0',
    tags:['2024']
}